name: CI Build

on:
  push:
    branches: [ "**" ]
  pull_request:

# A push and the matching pull_request event would otherwise both trigger
# the same compile twice; cancel the older run when a newer one starts.
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    uses: spring-ai-community/community-workflows/.github/workflows/ci-build.yml@main
    with:
      java-version: "21"
      maven-goals: clean compile -B
      skip-tests: true
