<?xml version="1.0" encoding="UTF-8"?>
<!--

    Copyright © 2025 Jemin Huh (hjm1980@gmail.com)

    Licensed under the Apache License, Version 2.0 (the "License");
    you may not use this file except in compliance with the License.
    You may obtain a copy of the License at

        http://www.apache.org/licenses/LICENSE-2.0

    Unless required by applicable law or agreed to in writing, software
    distributed under the License is distributed on an "AS IS" BASIS,
    WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
    See the License for the specific language governing permissions and
    limitations under the License.

-->
<configuration>

    <springProperty scope="context" name="APP_NAME" source="spring.application.name" defaultValue="spring-ai-playground"/>
    <springProperty scope="context" name="APP_BASE_DIR" source="spring.ai.playground.user-home" defaultValue="${user.home}"/>
    <property name="APP_HOME" value="${APP_BASE_DIR:-${user.home}}/${APP_NAME}"/>
    <property name="LOG_DIR" value="${APP_HOME}/logs"/>
    <property name="LOG_FILE" value="${LOG_DIR}/spring-ai-playground.log"/>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} [conv=%X{conversationId:-} msg=%X{userMessageId:-} traceId=%X{traceId:-} spanId=%X{spanId:-}] - %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="ROLLING_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_FILE}</file>

        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} [conv=%X{conversationId:-} msg=%X{userMessageId:-} traceId=%X{traceId:-} spanId=%X{spanId:-}] - %msg%n</pattern>
        </encoder>

        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_DIR}/spring-ai-playground.%d{yyyy-MM-dd}.%i.log.gz</fileNamePattern>
            <maxFileSize>10MB</maxFileSize>
            <maxHistory>30</maxHistory>
            <totalSizeCap>500MB</totalSizeCap>
        </rollingPolicy>
    </appender>

    <!-- Spring AI MCP autoconfig BeanPostProcessor warnings — no runtime effect. -->
    <logger name="org.springframework.context.support.PostProcessorRegistrationDelegate$BeanPostProcessorChecker" level="ERROR"/>

    <!-- mcp-stdio profile: stdout is the MCP JSON-RPC channel, so CONSOLE
         must not attach. Other profiles keep CONSOLE for normal dev/run.
         <springProfile> must wrap a top-level element (not be nested inside
         <root>), so we duplicate the <root> declaration per branch. -->
    <springProfile name="!mcp-stdio">
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
            <appender-ref ref="ROLLING_FILE"/>
        </root>
    </springProfile>
    <springProfile name="mcp-stdio">
        <root level="INFO">
            <appender-ref ref="ROLLING_FILE"/>
        </root>
    </springProfile>

</configuration>
